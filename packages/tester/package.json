{
  "name": "auph-tester",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "serve": "http-server . -o",
    "build": "node prepare && rollup -c",
    "watch": "rollup -w -c"
  },
  "dependencies": {
    "auph": "0.0.1"
  },
  "devDependencies": {
    "http-server": "^0.12.3",
    "rollup": "^2.53.2",
    "@rollup/plugin-node-resolve": "^13.0.4"
  }
}
